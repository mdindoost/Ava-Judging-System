# AVA JUDGING SYSTEM — SESSION CONTINUATION PROMPT
# Use this when starting a NEW Claude Code session to continue the build.
# Replace [PHASES DONE] and [NEXT PHASE] with actual values.

---

You are continuing the build of the Ava Judging System.

MANDATORY FIRST STEPS:
1. Run: cat CLAUDE.md
2. Run: cat CLAUDE_CODE_PROMPT.md  
3. Run: ls -la (see what already exists)
4. Do NOT regenerate files that already exist and are complete
5. Do NOT simplify anything
6. Continue exactly where the previous session left off

## Phases already completed:
[LIST COMPLETED PHASES HERE — e.g., "Phase 1: Foundation, Phase 2: Backend"]

## Current task:
Continue with [NEXT PHASE NAME] as described in CLAUDE_CODE_PROMPT.md.

## Quality reminder:
- Every function fully implemented (no TODOs, no placeholders)
- All user strings go through i18n.js
- All colors use CSS variables
- All API calls go through api.js
- All auth checks at top of every page
- Mobile-first, works on iPhone 375px width

Begin.
